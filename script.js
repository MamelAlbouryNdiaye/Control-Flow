const PI = 3.1415;
const radius = 5;
const area = PI * radius * radius; 
const plantSpace = 0.8; 
const initialPlants = 20;

const maxCapacity = Math.floor(area / plantSpace);// maximun plants capacity